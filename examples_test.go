// Test individual: go test -run Suma
// Test all: go test
// Test all with verbose: go test -v
// Test all with coverage: go test -cover
package main

/*
import "testing"

func TestSuma(t *testing.T) {
	// Prueba con números positivos
	resultado := Suma(3, 5)
	if resultado != 8 {
		t.Errorf("Suma(3, 5) = %d; se esperaba 8", resultado)
	}

	// Prueba con un número positivo y otro negativo
	resultado = Suma(-1, 1)
	if resultado != 0 {
		t.Errorf("Suma(-1, 1) = %d; se esperaba 0", resultado)
	}

	// Prueba con ambos números negativos
	resultado = Suma(-3, -5)
	if resultado != -8 {
		t.Errorf("Suma(-3, -5) = %d; se esperaba -8", resultado)
	}
}

func TestEsPar(t *testing.T) {
	// Prueba con un número par
	if !EsPar(2) {
		t.Errorf("EsPar(2) debería ser verdadero")
	}

	// Prueba con un número impar
	if EsPar(3) {
		t.Errorf("EsPar(3) debería ser falso")
	}
}
*/
